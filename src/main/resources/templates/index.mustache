
<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Site</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="../static/css/main.css">
</head>
<body>
<div class="container-fluid">
    <!-- 첫 번째 행 -->
    <div class="row align-items-center">
        <div class="col-md-8">
            <!-- 여기에 내용을 추가 -->
        </div>
        <div class="col-md-3" style="text-align: right;">
            {{#isLoggedIn}}
                <a href="/mypage" style="text-decoration: none; color: inherit;">마이페이지</a>
            {{/isLoggedIn}}
            {{^isLoggedIn}}
                <a href="/login" style="text-decoration: none; color: inherit;">로그인</a>
            {{/isLoggedIn}}
        </div>
        <div class="col-md-1" style="text-align: right;">
            {{#isLoggedIn}}
                <a href="/logout" style="text-decoration: none; color: inherit;">로그아웃</a>
            {{/isLoggedIn}}
        </div>
    </div>


    <!-- 두 번째 행 -->
    <div class="row align-items-center my-4">
        <!-- 이미지 -->
        <div class="col-md-4 text-right" style="display: flex; align-items: center; justify-content: right;">
            <img src="/img/logo.png" alt="Logo" style="width: 50px; height: auto;">
        </div>

        <!-- 검색 폼 -->
        <div class="col-md-8" style="display: flex; align-items: center;">
            <form action="/search" method="GET" style="display: flex; align-items: center; width: 100%;">
                <input type="text" name="query" class="form-control me-2" placeholder="검색어를 입력하세요" style="max-width: 400px; margin-right: 10px;">
                <button type="submit" class="btn btn-primary">검색</button>
            </form>
        </div>
    </div>
    <!-- 세 번째 행 -->
    <div class="row mt-4">
        <div class="col-md-12 text-center">
            <div id="carouselExample" class="carousel slide" data-bs-ride="carousel">
                <div class="carousel-inner">
                    <div class="carousel-item active">
                        <img src="img/image1.png" class="d-block w-100" alt="Image 1">
                    </div>
                    <div class="carousel-item">
                        <img src="img/image2.png" class="d-block w-100" alt="Image 2">
                    </div>
                    <div class="carousel-item">
                        <img src="img/image3.png" class="d-block w-100" alt="Image 3">
                    </div>
                </div>
                <button class="carousel-control-prev" type="button" data-bs-target="#carouselExample" data-bs-slide="prev">
                    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                    <span class="visually-hidden">Previous</span>
                </button>
                <button class="carousel-control-next" type="button" data-bs-target="#carouselExample" data-bs-slide="next">
                    <span class="carousel-control-next-icon" aria-hidden="true"></span>
                    <span class="visually-hidden">Next</span>
                </button>
            </div>
        </div>
    </div>
</div>


<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
